

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne V.sans google agenda
```
^button-nhhy
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0

___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-12-14') return true; \
if (task.file.filenameWithoutExtension === "2024-12-14" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-12-14') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2024-12-14 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2024-12-15) AND date(due_at) >= date(2024-12-14)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___

# actions

# notes (peut-être) à revoir une fois


# Parler de carte micro se noel
- [X] (créé à 21h19 le 13 décembre à 54,2 secondes)  [completion:: 2024-12-30]


# Demander à Michel ça veut dire quoi les blanches avec les doubles
- [X] Lui envoyer une photo en lui demandant  [completion:: 2024-12-30]


# Voir pour sandwich pour tournoi ping
- [X] (créé à 23h16 le 13 décembre à 15,7 secondes)  [completion:: 2024-12-30]


# Voir pour rendu payan
- [X] (créé à 0h2 le 14 décembre à 1,4 secondes)  [completion:: 2024-12-30]


# Utiliser CTRL pour sélectionner plusieurs notes puis x pour avoir plusieurs notes sur la même portée
- [X] (créé à 10h0 le 14 décembre à 53,1 secondes)  [completion:: 2024-12-30]
