



> [!tip] Daily Classical Music
> [Chromatic Fantasia and Fugue in D minor, BWV 903 by Bach](https://www.youtube.com/watch?v=SNWOhm5iXxs)

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne
```
^button-huhm
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-11-21') return true; \
if (task.file.filenameWithoutExtension === "2024-11-21" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-11-21')) return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"Modèles (inclus)/modèle templater pour boîte de réception" and -"Modèles (inclus)/modèle pour module principal"
where due_at != null AND date(due_at) <= date(2024-11-22) AND date(due_at) >= date(2024-11-21)
sort due_at desc
```

```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___
# évènements de la journée
```gEvent
date: 2024-11-21
type: day
```
___

# notes (peut-être) à revoir une fois

# Résoudre problème modèle dataview note quotidienne et après régler bouton rafraîchir
- [X] (créé à 22h55 le 20 novembre à 55,4 secondes)  [start:: 2024-11-22]  [completion:: 2024-11-22]


# Revoir boutons page d'accueil modèle gtd si ils peuvent servir
- [X] (créé à 23h12 le 20 novembre à 19,5 secondes)  [start:: 2024-11-22]  [completion:: 2024-11-22]
# partager cartes pour Luxey
- [X] changer notation en utilisant starti etc... chercher pour remplacement  [completion:: 2024-11-24]
