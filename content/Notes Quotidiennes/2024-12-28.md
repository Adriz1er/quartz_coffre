

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne V.sans google agenda
```
^button-nhhy
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0

___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-12-28') return true; \
if (task.file.filenameWithoutExtension === "2024-12-28" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-12-28') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2024-12-28 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2024-12-29) AND date(due_at) >= date(2024-12-28)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___

# actions
- [X] Proposer à #👤/Michel ecco ridente un cielo, largo la factotum, et zitti zitti piano et dire que je suis ouvert à d'autres  [completion:: 2024-12-30]
- [X] aa adrien est-ce que tu peux demander à urso s'il arrive à accèder à l'asa ou y'a un mot de passe parce que ça fait 3 jours que j'essaye mais chaque fois je trouve un mot de passe et il faut que je le configure  [completion:: 2024-12-30]

# notes (peut-être) à revoir une fois


# Ajouter Ahmed sur gît et demander à ce qu'il push
- [X] Ahmed me donne son git  [start:: 2024-12-30]  [completion:: 2025-01-11]


# Voir avec maman pour acheter housse pour casque
- [-] (créé à 11h18 le 28 décembre à 27,2 secondes)  [cancelled:: 2024-12-30]
