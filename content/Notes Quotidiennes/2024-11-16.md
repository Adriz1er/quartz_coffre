



> [!tip] Daily Classical Music
> [Cantata #82 'Ich habe genug' by Bach](https://www.youtube.com/watch?v=Q_5DG9BD-SU)

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne
```
^button-huhm
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-11-16') return true; \
if (task.file.filenameWithoutExtension === "2024-11-16" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-11-16')) return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"Modèles (inclus)/modèle templater pour boîte de réception" and -"Modèles (inclus)/modèle pour module principal"
where due_at != null AND date(due_at) <= date(2024-11-17) AND date(due_at) >= date(2024-11-16)
sort due_at desc
```

```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___
# évènements de la journée
```gEvent
date: 2024-11-16
type: day
```
___

# notes (peut-être) à revoir une fois

# mettre dual boot sur mon ordi pour voir si au niveau clavier c'est mieux
- [-] (créé à 10h40 le 10 novembre à 46,8 secondes)  [cancelled:: 2024-11-24]


# Jouer à undercover avec Tomoyasu et 16
- [X] (créé à 15h10 le 10 novembre à 14,1 secondes)  [start:: 2024-12-06]  [completion:: 2024-12-30]


# Répondre à Lola avec l'impression du tourbillon d'idée lors de la revue (souvent ça me pousse à faire des choses),
- [X] Relire son message  [completion:: 2024-11-24]


# Prendre en photo carte uni et mettre en favoris
- [X] (créé à 10h51 le 16 novembre à 26,8 secondes)  [completion:: 2024-11-24]


# Grouper liste en attente avec group by function si c'est dans boite de réception avec emoticone et sinon ____ 
- [X] (créé à 11h14 le 16 novembre à 28,8 secondes)  [completion:: 2024-11-16]
