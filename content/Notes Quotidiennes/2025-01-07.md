



> [!tip] Daily Classical Music
> [L'estro Armonico, op. 3 by Vivaldi](https://www.youtube.com/watch?v=c6_Z5lpc1g0)

```button
name Ajouter un élément
type command
action QuickAdd: test2
```
^button-g0kw
note quotidienne du jour précédent : [[2025-01-06]]
___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2025-01-07') return true; \
if (task.file.filenameWithoutExtension === "2025-01-07" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2025-01-07') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2025-01-07 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2025-01-08) AND date(due_at) >= date(2025-01-07)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___
## éléments reportés à ce jour

- [!] Mettre une icone pour date inserter dans barre d'outils mobile sur modèle
- [!] Faire le point sur les saé  [start:: 2025-01-08]
- [X] Régler problèmes d'ouverture de panneaux depuis barre latérale mobile sur modèle et sur config  [completion:: 2025-01-07]
- [!] faire en sorte que les notes qu'on ouvre avec le plugin ouvrir file with s'ouvrent dans un nouveau panneau à droite sur ma config et sur modèle ; et enlever onglet suivant et délai dans macro de chaque bouton
- [!] Demander quoi faire pour le sweat à maman en lui plus montrant  [start:: 2025-01-08]