

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne V.sans google agenda
```
^button-nhhy
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0

___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-12-27') return true; \
if (task.file.filenameWithoutExtension === "2024-12-27" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-12-27') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2024-12-27 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2024-12-28) AND date(due_at) >= date(2024-12-27)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___

# actions
- [ ] Demander à #👤/Michel comment aiguiser la lame du crilef  [start:: 2025-01-03]

# notes (peut-être) à revoir une fois


# Demander à Michel s'il pense que eboullanté les anches sert à pas que les enzymes attaque le bois sinon chercher 
- [ ] (créé à 16h36 le 26 décembre à 36,9 secondes)  [start:: 2025-01-03]


# Voir si lampe s'allume bien automatiquement avant réveil
- [X] (créé à 17h43 le 26 décembre à 30,7 secondes)  [completion:: 2024-12-30]


# Parler à 16R de l'histoire au niveau du fait que je pourrai lui filer mon ampoule 
- [-] Si je prend ampoule Ikea et lui dire que j'hésite (créé à 11h35 le 27 décembre à 47,8 secondes)  [start:: 2025-01-02]  [cancelled:: 2025-01-02]


# Régler compte duolingo Thomas qui est en privé
- [X] (créé à 17h0 le 27 décembre à 57,8 secondes)  [completion:: 2024-12-30]


# Proposer à Michel ecco ridente un cielo, largo la factotum, et zitti zitti piano et dire que je suis ouvert à d'autres 
- [X] (créé à 20h23 le 27 décembre à 11,8 secondes)  [completion:: 2024-12-27]
