



> [!tip] Daily Classical Music
> [Die Kunst der Fuge (The Art of Fugue), BWV 1080 by Bach](https://www.youtube.com/watch?v=N6sUlZa-IrU)

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne
```
^button-huhm
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-11-24') return true; \
if (task.file.filenameWithoutExtension === "2024-11-24" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-11-24')) return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"Modèles (inclus)/modèle templater pour boîte de réception" and -"Modèles (inclus)/modèle pour module principal"
where due_at != null AND date(due_at) <= date(2024-11-25) AND date(due_at) >= date(2024-11-24)
sort due_at desc
```

```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___
# évènements de la journée
```gEvent
date: 2024-11-24
type: day
```
___

# notes (peut-être) à revoir une fois

# Republier modèle et envoyer sms à matthieu combaz et voir avec 16 pour lui faire installer #ordi
- [X] Utiliser le fait que quand on part et revient sur un onglet ça active l'éditeur ce qui peut permettre de changer de dossier note dans modèle ; et voir pour faire bouton pour créer directement une note récurrente reportée ; faire un bouton projet partagé  [start:: 2024-11-27]  [completion:: 2024-12-01]
- [X] enlever bouton pour saisir un intrant de page d'accueil  [start:: 2024-11-27]  [completion:: 2024-12-01]
