

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne V.sans google agenda
```
^button-nhhy
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0

___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-12-19') return true; \
if (task.file.filenameWithoutExtension === "2024-12-19" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-12-19') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2024-12-19 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2024-12-20) AND date(due_at) >= date(2024-12-19)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___

# actions

# notes (peut-être) à revoir une fois


# Commencer série mr beast
- [?] Sinon reporter à demain  [repeat:: every day when done]  [start:: 2025-01-02]


# Réviser td1 et td2 pour iot sur moodle
- [X] (créé à 12h10 le 18 décembre à 56,5 secondes)  [start:: 2024-12-20]  [completion:: 2024-12-30]


# Montrer à Mathias cartes que 16R a obtenu récemment 
- [X] (créé à 19h32 le 18 décembre à 32,9 secondes)  [completion:: 2024-12-19]


# Relire luxey
- [X] (créé à 21h19 le 18 décembre à 5,0 secondes)  [completion:: 2024-12-19]
