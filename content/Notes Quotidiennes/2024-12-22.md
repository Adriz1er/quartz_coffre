

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne V.sans google agenda
```
^button-nhhy
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0

___
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-12-22') return true; \
if (task.file.filenameWithoutExtension === "2024-12-22" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-12-22') && task.heading !== "évènements de la journée") return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes reportées à ce jour
```dataview
table
from #2024-12-22 and -"Notes Quotidiennes"
```
```button
name Rafraîchir
type line(38) text
action ```dataview
replace [38,38]
```
^button-h78d
## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"roadmap"
where due_at != null AND date(due_at) <= date(2024-12-23) AND date(due_at) >= date(2024-12-22)
sort due_at desc
```
```button
name Rafraichir
type line(50) text
action ```dataview
replace [50,50]
```
^button-dhw3
```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___

# actions

# notes (peut-être) à revoir une fois


# Trouver comment adapter l'affichage aux dimensions de mon téléphone
- [X] (créé à 18h32 le 21 décembre à 42,5 secondes)  [completion:: 2024-12-30]


# Demander à maman d'acheter getting things done avec pass culture et voir pour le donner à Michel et prendre un selfie et l'envoyer à Lola
- [-] (créé à 22h55 le 21 décembre à 59,8 secondes)  [cancelled:: 2024-12-30]


# Voir avec Thomas pour qu'il quitte la classe duolingo car ça va lui laisser les vies à l'infini (quasi sur)
- [X] (créé à 14h34 le 22 décembre à 27,4 secondes)  [start:: 2024-12-31]  [completion:: 2025-01-01]
