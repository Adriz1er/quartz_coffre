
```tasks
tag does not include #noté 
group by function task.file.filenameWithoutExtension + (task.hasHeading ? (' | ' + task.heading) : '')
sort by filename 
sort by heading
status.name does not include vérification
hide created date
path includes dropbox/Dropbox/pour projet thalès
```