
```tasks
tag does not include #noté 
tag does not include #inclus 
status.name includes en attente
status.name does not include Speech bubble
status.name does not include Question
status.name does not include Todo
status.name does not include Idea
status.type is not DONE
status.type is not Cancelled
sort by created
group by function    \
if (task.file.filenameWithoutExtension === '📥') { \
	return task.file.filenameWithoutExtension \
} \
else { \
	return '' \
}
path does not include archives
```