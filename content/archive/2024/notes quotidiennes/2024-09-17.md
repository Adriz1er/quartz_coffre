



> [!tip] Daily Classical Music
> [Symphony #5, op. 50 by Nielsen](https://www.youtube.com/watch?v=pOohguxFpKY)

```button
name Ajouter une action
type command
action QuickAdd: pour actions dans note quotidienne
```
^button-huhm
```button
name Ajouter un projet
type command
action QuickAdd: pour projets dans note quotidienne
```
^button-13m0
# éléments reportés
```tasks
tag does not include #noté 
sort by filename 
sort by urgency 
sort by created 
group by function    \
	if (task.heading == 'évènements de la journée') return task.file.filenameWithoutExtension ; \
    return task.file.filenameWithoutExtension + (task.hasHeading ? (' > ' + task.heading) : '');
filter by function \
if (task.file.filenameWithoutExtension === "📥" && (task.status.type === "DONE" || task.status.type === "CANCELLED")) return false; \
if (task.start.formatAsDate() === '2024-09-17') return true; \
if (task.file.filenameWithoutExtension === "2024-09-17" && (task.start.formatAsDate() === '' || task.start.formatAsDate() === '2024-09-17')) return true; \
return false;
hide start date
status.name does not include info
status.name does not include location
hide task count
```

## notes récurrentes reportées à ce jour
```dataview
table repeat as se_répète_tout_les
from "" and -"Modèles (inclus)/modèle templater pour boîte de réception" and -"Modèles (inclus)/modèle pour module principal"
where due_at != null AND date(due_at) <= date(2024-09-18) AND date(due_at) >= date(2024-09-17)
sort due_at desc
```

```button
name Revoir les notes récurrentes reportées
type command
action Repeat: Review due notes
```
^button-y45u
___
# évènements de la journée
```gEvent
date: 2024-09-17
type: day
```
___

# notes (peut-être) à revoir une fois

# Écrire qu'il faut phraser le plus possible dans la manière que l'on veut jouer en haut du sous-titre que j'ai envoyé à Lola et mettre à jour note et index sur site rapidement #ordinateur
- [X] (créé à 22h46 le 16 septembre à 12,1 secondes)  [completion:: 2024-09-23]
# mettre undercover sur page d'accueil
- [X] (créé à 13h52 le 17 septembre à 37,0 secondes)  [completion:: 2024-09-23]


# Ecrire function maths
- [X] (créé à 7h7 le 17 septembre à 10,9 secondes)  [completion:: 2024-09-23]
# tenir au courant Mathieu Combaz quand j'ai mis à jour le site courtement
- [X] (créé à 15h13 le 17 septembre à 35,6 secondes)  [completion:: 2024-09-23]
